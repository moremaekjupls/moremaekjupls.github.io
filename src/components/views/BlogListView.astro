---
import { getCollection } from 'astro:content';
import { t, readingMinutes, readingLabel } from '../../i18n/ui';
const posts = (await getCollection('blog', (e) => e.data.lang === 'en' && !e.data.draft))
  .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());
const fmt = (d: Date) => new Intl.DateTimeFormat('en-US', { year: 'numeric', month: 'short', day: 'numeric' }).format(d);
const titleLines = (t('blog.title') as string).split('\n');
---
<section class="page-intro">
  <div class="container">
    <span class="eyebrow">{t('blog.eyebrow')}</span>
    <h1 class="display">{titleLines.map((l, i) => (<>{l}{i < titleLines.length - 1 && <br />}</>))}</h1>
    <p class="body body--muted" style="margin-top:24px;">{t('blog.intro')}</p>
  </div>
</section>

<section class="section">
  <div class="container">
    {posts.length === 0 ? (
      <p class="muted">{t('blog.empty')}</p>
    ) : (
      <div class="post-list">
        {posts.map((p, i) => (
          <a class={`post-row${i === 0 ? ' post-row--lead' : ''}`} href={`/blog/${p.data.transId}`}>
            <div class="post-row-date">{fmt(p.data.pubDate)}<span class="post-row-read">{readingLabel(readingMinutes(p.body))}</span></div>
            <div>
              <h2 class="post-row-title">{p.data.title}</h2>
              <p class="post-row-excerpt">{p.data.excerpt}</p>
            </div>
          </a>
        ))}
      </div>
    )}
  </div>
</section>
